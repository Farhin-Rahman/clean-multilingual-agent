# --- Core app ---
streamlit>=1.34
python-dotenv>=1.0.1

# --- Data / finance / HTML parsing ---
pandas>=2.1
numpy>=1.25
yfinance>=0.2.38
requests>=2.31
beautifulsoup4>=4.12
lxml>=4.9
html5lib>=1.1

# --- LLM / orchestration (Groq via LangChain) ---
langgraph>=0.2.20
langchain-core>=0.2.10
langchain-groq>=0.1.5
groq>=0.5.0
pydantic>=2.6
typing-extensions>=4.9

# --- NLP & translation ---
transformers>=4.41
sentencepiece>=0.1.99
langdetect>=1.0.9
# transformers needs a backend; CPU PyTorch works fine:
torch>=2.2.0

# --- PDF processing ---
PyMuPDF>=1.24.0  # (package name is PyMuPDF even though you import fitz)

# --- Web search ---
duckduckgo-search>=4.9.0

# --- Audio I/O (optional, enable if you use voice/tts) ---
gTTS>=2.5.1
streamlit-mic-recorder>=0.0.8

# --- Speech-to-text (optional; used by faster-whisper in app.py) ---
faster-whisper>=0.10.0

# --- Optional external data providers (the app degrades gracefully without them) ---
finnhub-python>=2.4.0
fredapi>=0.5.1
